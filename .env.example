NODE_ENV=development
PORT=10000
DATABASE_URL=
DATABASE_SSL=true
# Required in production: long random server-only value used as a password-hash pepper.
AUTH_PEPPER=replace-with-a-long-random-secret
# Optional production email verification adapter. Without one, no verification URL is exposed in production.
EMAIL_PROVIDER=
EMAIL_FROM=learn@gentsacademy.net
