# Browser audit findings

The single-page GentsAcademy app rendered successfully on the local running service. The home view showed the existing dark interface with navy gradients, gold typography and actions, Playfair Display headings, Inter body text, responsive cards, and the original promise “Education Is The New Freedom.” The navigation exposed Home, Catalogue, Pathways, My learning, and Start learning, with no language selector or duplicate-language route.

The home page displayed data-driven totals of 41 published courses, 6 academic areas, and 4 career pathways. The department section exposed Business and Entrepreneurship, IT and Technology, Hospitality Management, Tourism and Destination Management, and Interdisciplinary. Featured course cards loaded image assets, primary department labels, course codes, level, duration, workload, descriptions, and View course actions.

The catalogue route rendered the centralized registry with a search field, department selector, level selector, pathway selector, filter action, clear-filters action, and 41 course cards. Cards used the preserved dark card style and department-specific badges. The browser audit found no loading failure or missing application shell in the tested views.

The course detail view rendered the strategy-required metadata: primary department and code, level, duration, workload, course certificate type, measurable learning outcomes, four expandable modules, 20/30/20/30 assessment weighting with a 70% pass threshold, prerequisites and device expectations, capstone evidence, career relevance, low-bandwidth support, certificate rules, related courses, and a free enrollment action.

The enrollment route rendered a focused, labelled form for full name and email with a clear confirmation action and a low-bandwidth/privacy notice. The form was keyboard/focus-auditable and kept the established dark card, gold button, and serif-heading style.

The learner dashboard rendered a dedicated email-based lookup form and retained the same visual system. The underlying API flow was already verified with an enrollment, learner lookup, and progress update, so the page is ready to display active enrollments and module completion state.

The pathways view rendered four stackable programmes: Hotel Operations, Digital Tourism, Hospitality Entrepreneurship, and Responsible Service-Sector Leadership. Each pathway exposed its purpose, six recommended courses, and a View pathway action, confirming that the catalogue is no longer a flat list of unrelated courses.

Render deployment audit: the Render dashboard redirected to its sign-in page, so no authenticated Render account or existing service could be inspected or deployed from this session. The repository does contain `render.yaml` with the Node web service, PostgreSQL database, health check, and required environment variable definitions. The GitHub repository is already pushed and ready for Render’s Blueprint deployment or an existing Render service to be connected.

Render deployment progress: after the user authenticated and confirmed deployment, Render accepted the Blueprint from `brimaabrahamfuller-blip/gentsacademy` at commit `c16f62a` and created Blueprint `gentsacademy` with ID `exs-d9vg63tg1s2s73fefbn0`. The PostgreSQL resource `gentsacademy-db` was created successfully and showed status `available` on Render’s Free plan. The Blueprint sync page continued to show the web service creation as running while the service was still provisioning; the public web-service URL was not yet exposed in the available status view.

Live Render verification: `https://gentsacademy.onrender.com` is now live, serves the new GentsAcademy HTML shell, and `/api/health` reports `ok: true` with `database: postgresql`. Render logs confirm the service started with `npm start`, passed health checks, and is live. A routing discrepancy was found on the deployed service: `/api/courses/` and `/api/courses/tec-002/` work, while the no-trailing-slash forms `/api/courses` and `/api/courses/tec-002` intermittently returned 404 even though local Express tests accept both forms. This needs a small route-normalization patch so the browser client’s no-slash API calls work reliably in production.

Render redeploy status: the service dashboard is live at `https://gentsacademy.onrender.com`, but the deployment list still shows the prior successful commit `c16f62a` while the new `b9c7ad3` commit is waiting for automatic deployment. The service URL and Blueprint-managed configuration remain active.

Final cross-platform deployment audit: GitHub `main` contains only the intentional application files—`public/index.html`, `public/styles.css`, `public/app.js`, `server.js`, `data/catalogue.js`, tests, and deployment configuration. The old root-level HTML pages and legacy `css/` and `js/` folders are gone. The phrase “single language” refers to the English learner-facing content and JavaScript application/backend implementation; `index.html` and `styles.css` remain necessary browser assets and are not unused legacy code.

The actual 404 source was Netlify. Its previous ready deploy was created from the repository root with no publish directory and reported “No redirect rules processed.” After the refactor, the root no longer contained the old `index.html`, so `gentsacademy.netlify.app` returned Netlify’s Page Not Found screen. Render was already healthy and serving the application and PostgreSQL API correctly.

Remediation: added `netlify.toml` with `publish = "public"`, a forced `/api/*` proxy to `https://gentsacademy.onrender.com/api/:splat`, and a SPA fallback to `/index.html`; pushed commit `feff519`; deployed the corrected repository to the existing Netlify site; and verified `https://gentsacademy.netlify.app/`, `/api/health`, and `/api/catalogue-summary` all return HTTP 200. Netlify’s corrected deploy `6a7f06f9a0b16c39e4cf7244` is ready and reports that two redirect rules were processed without errors.
